<Window x:Class="TimeManagementApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TimeManagementApp.ViewModels"
        Title="Login" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" Grid.Row="0" Margin="0 0 0 5"/>
        <TextBox Grid.Row="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                 Width="250" Height="30" HorizontalAlignment="Left"/>

        <TextBlock Text="Password:" Grid.Row="2" Margin="0 10 0 5"/>
        <PasswordBox Grid.Row="3" Width="250" Height="30" HorizontalAlignment="Left"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0 20 0 0">
            <Button Content="Login" Width="100" Margin="0 0 10 0"
                    Command="{Binding LoginCommand}" />
            <Button Content="Register" Width="100"
                    Command="{Binding NavigateToRegisterCommand}" />
        </StackPanel>
    </Grid>
</Window>
