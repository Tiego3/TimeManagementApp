<Window x:Class="TimeManagementApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Username:"/>
            <TextBox Name="UsernameTextBox"/>
            <TextBlock Text="Password:" Margin="0,10,0,0"/>
            <PasswordBox Name="PasswordBox"/>
            <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
                <Button Content="Login" Click="Login_Click" Width="100" Margin="0,0,10,0"/>
                <Button Content="Register" Click="Register_Click" Width="100"/>
            </StackPanel>
            <TextBlock Name="MessageTextBlock" Foreground="Red" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
